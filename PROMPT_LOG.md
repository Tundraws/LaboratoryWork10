# Prompt Log

## Задание С3: Валидация входных данных в Go

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Создай Go-сервис на Gin для ЛР №10. Нужны структуры запроса с обязательной валидацией через binding, эндпоинт авторизации и защищённый эндпоинт для приёма сложного JSON."
**Результат:** Получил каркас Go-сервиса с `Gin`, моделями `LoginRequest` и `ProcessRequest`, а также обработчиками `/auth/token` и `/api/process`.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Добавь table-driven unit tests для валидации запросов и проверки защищённого эндпоинта через httptest."
**Результат:** Добавлены Go-тесты для успешных и ошибочных сценариев: невалидное тело, отсутствие токена, неверные credentials, корректный JSON.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: зафиксирована версия `gin`, совместимая с локальным Go toolchain.
- Время: ~20 мин

---

## Задание С5: Передача сложных структур данных (JSON) между сервисами

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Сделай FastAPI-сервис, который принимает сложный JSON, валидирует его через pydantic и пересылает в Go-сервис через httpx."
**Результат:** Создан Python-сервис с моделью `ProcessRequest`, клиентом `GoServiceClient` и эндпоинтом `/api/forward`, который передаёт JSON в Go.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Добавь асинхронные pytest-тесты для Python-сервиса без внешнего запуска серверов: используй ASGITransport и фейковый клиент Go."
**Результат:** Подготовлены асинхронные тесты, которые проверяют успешную пересылку JSON, ошибки валидации и работу health endpoint.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: в окружении Windows путь проекта содержит скобки, поэтому для запуска тестов использовалась локальная папка `.pydeps` вместо обычного `venv`.
- Время: ~25 мин

---

## Задание С7: Graceful shutdown в обоих сервисах

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Добавь graceful shutdown в Go через SIGINT/SIGTERM и context.WithTimeout. Сделай это тестируемым."
**Результат:** Логика запуска Go-сервера вынесена в функцию `run`, добавлен `http.Server.Shutdown`, написаны тесты на корректное завершение и ошибочный запуск.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Добавь корректное завершение Python-сервиса через lifespan и закрытие httpx.AsyncClient."
**Результат:** Во FastAPI добавлен `lifespan`, который создаёт и закрывает клиент Go-сервиса без утечек ресурсов.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: отдельно добавлены тесты на shutdown для Go, чтобы покрыть ветки с сигналами и дефолтной конфигурацией.
- Время: ~15 мин

---

## Задание П3: JWT-аутентификация в Go и проверка токенов в Python

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Реализуй генерацию и проверку JWT в Go-сервисе на базе golang-jwt/jwt, добавь middleware авторизации."
**Результат:** Добавлен пакет `internal/auth`, генерация токена по логину и middleware, проверяющий `Authorization: Bearer <token>`.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Пусть Python не просто проксирует токен, а локально валидирует его через PyJWT перед отправкой запроса в Go."
**Результат:** Добавлена функция `validate_jwt_token`, Python-сервис извлекает subject из токена и возвращает его в ответе как `verified_subject`.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: согласован единый `JWT_SECRET` между Go, Python и Docker Compose через environment variables.
- Время: ~20 мин

---

## Задание П5: Развёртывание в Docker Compose с общей сетью

### Промпт 1
**Инструмент:** Codex (GPT-5)
**Промпт:** "Добавь минимальные Dockerfile для Go и Python сервисов и общий docker-compose.yml с общей сетью и healthcheck."
**Результат:** Добавлены `Dockerfile` для обоих сервисов и `docker-compose.yml`, где Python обращается к Go по имени сервиса `go-service`.

### Промпт 2
**Инструмент:** Codex (GPT-5)
**Промпт:** "Сделай конфигурацию Docker аккуратной: убери лишний build context и исключи локальные кеши."
**Результат:** Добавлены `.dockerignore` для обоих сервисов, чтобы в образы не попадали `.venv`, `.pydeps`, `.cache` и test-артефакты.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: Docker не был установлен в исходной среде, поэтому конфигурация подготовлена заранее, а фактический запуск запланирован после установки Docker Desktop.
- Время: ~15 мин

---

## Общий итог

- Всего промптов: 10
- Что дополнительно проверено вручную: чистота `git ls-files`, отсутствие артефактов в индексе, прохождение `go test ./...` и `pytest`, корректность `README.md`.
- Что осталось проверить после установки Docker: `docker compose up --build`.
- Общее время: ~95 мин
